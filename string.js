// concat
const a = 'hello, how are you ?'
const b = ' How about your course ?'
const jora = a.concat(b)
console.log(jora)

// join 
// If you have an array with multiple lines you can join this as a string. Let's see how is it. 

const lines = ['Hey.........','how are you ?','how about your work ?']
const string = lines.join(',')
console.log(string)

// slice sting
const slice = string.slice(14, 20)
console.log(slice);

// subtract is same as slice
